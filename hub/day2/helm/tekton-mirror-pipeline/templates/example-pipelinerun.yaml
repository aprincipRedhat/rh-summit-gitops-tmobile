{{- if .Values.pipelineRun.example.enabled }}
apiVersion: tekton.dev/v1
kind: PipelineRun
metadata:
  generateName: {{ .Values.pipelineRun.example.generateName | quote }}
  namespace: {{ .Values.namespace }}
spec:
  pipelineRef:
    name: {{ .Values.pipeline.name }}
  taskRunTemplate:
    serviceAccountName: {{ .Values.serviceAccount.name }}
  params:
    - name: dest-registry
      value: {{ .Values.pipelineRun.example.destRegistry | quote }}
    - name: extra-flags
      value: {{ .Values.pipelineRun.example.extraFlags | quote }}
  workspaces:
    - name: imageset
      configMap:
        name: {{ .Values.rbac.configMapName }}
    - name: registry-auth
      secret:
        secretName: {{ .Values.rbac.secretName }}
{{- end }}
