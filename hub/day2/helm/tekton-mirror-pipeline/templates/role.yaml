apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ printf "%s-role" .Values.tektonMirror.serviceAccount.name }}
  namespace: openshift-pipelines
rules:
  - apiGroups: [""]
    resources: ["configmaps"]
    resourceNames: [{{ .Values.tektonMirror.rbac.configMapName | quote }}]
    verbs: ["get", "list", "watch"]
  - apiGroups: [""]
    resources: ["secrets"]
    resourceNames: [{{ .Values.tektonMirror.rbac.secretName | quote }}]
    verbs: ["get", "list", "watch"]
