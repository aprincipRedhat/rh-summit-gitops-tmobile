apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ printf "%s-binding" .Values.tektonZtp.serviceAccount.name }}
  namespace: openshift-pipelines
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ printf "%s-role" .Values.tektonZtp.serviceAccount.name }}
subjects:
  - kind: ServiceAccount
    name: {{ .Values.tektonZtp.serviceAccount.name }}
    namespace: openshift-pipelines
