apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: {{ .Values.acm.operatorGroup.name }}
  namespace: {{ .Values.acm.namespace }}
spec:
  targetNamespaces:
    - {{ .Values.acm.namespace }}
