apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: {{ .Values.mce.operatorGroup.name }}
  namespace: {{ .Values.mce.namespace }}
spec:
  targetNamespaces:
    - {{ .Values.mce.namespace }}
