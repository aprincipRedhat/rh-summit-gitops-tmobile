# Generic Opaque Secret — substitute ${NAME}, ${NAMESPACE}, ${USERNAME}, ${PASSWORD}.
apiVersion: v1
kind: Secret
metadata:
  name: ${NAME}
  namespace: ${NAMESPACE}
type: Opaque
stringData:
  username: ${USERNAME}
  password: ${PASSWORD}
