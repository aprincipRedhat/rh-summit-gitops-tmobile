# Generic ClusterIP Service — substitute ${NAME}, ${NAMESPACE}, ${APP_LABEL}.
apiVersion: v1
kind: Service
metadata:
  name: ${NAME}
  namespace: ${NAMESPACE}
spec:
  selector:
    app: ${APP_LABEL}
  ports:
    - name: http
      port: 8080
      targetPort: 8080
