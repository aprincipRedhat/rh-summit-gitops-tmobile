# Defaults — hub overrides via hub-clusters/day2/99-environments/.../values.yaml (tektonZtp)
tektonZtp:
  git:
    cloneDepth: 50
  manualApproval:
    timeoutSeconds: 7200
  clusterValidation:
    timeoutSeconds: 3600
  # Optional Secret in openshift-pipelines with VAULT_ADDR and VAULT_TOKEN for Redfish MAC discovery.
  vault:
    secretName: ""
