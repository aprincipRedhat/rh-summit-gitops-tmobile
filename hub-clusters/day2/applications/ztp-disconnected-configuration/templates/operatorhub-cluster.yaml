{{- if and .Values.ztpDisconnected.enabled .Values.ztpDisconnected.operatorHub.enabled }}
apiVersion: config.openshift.io/v1
kind: OperatorHub
metadata:
  name: cluster
spec:
  disableAllDefaultSources: {{ .Values.ztpDisconnected.operatorHub.disableAllDefaultSources }}
{{- end }}
