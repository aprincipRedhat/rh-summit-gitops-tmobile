# Override via hub-clusters/day2/99-environments/<env>/<site>/<hub>/values.yaml → gitopsReposConfig
gitopsReposConfig:
  enabled: false
  defaultNamespace: openshift-gitops
  vaultStaticSecrets: []
  # Example — requires VaultAuth (etc.) in the same namespace as each VaultStaticSecret:
  # - name: rh-summit-gitops-repo
  #   namespace: openshift-gitops
  #   vaultAuthRef: vault-auth-gitops
  #   mount: kv-v2
  #   path: secret/data/gitops/rh-summit
  #   type: kv-v2
  #   refreshAfter: 1m
  #   destination:
  #     name: rh-summit-gitops-repo-creds
  #     create: true
  #     labels:
  #       argocd.argoproj.io/secret-type: repository
